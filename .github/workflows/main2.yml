name: CI
on:
  pull_request:
             branches: ["main"]
  workflow_dispatch: 
jobs:
  build: 
    runs-on: ubuntu-latest
    steps: 
      - uses: actions/checkout@v3
      - name: Greet user
        run: echo welcome to Actions 
  test:
     runs-on: ubuntu-latest
     steps: 
      - uses: actions/checkout@v3
      - name: Greet user
        run: echo welcome to Actions
  Deploy: 
      runs-on: ubuntu-latest
      steps: 
      - uses: actions/checkout@v3
      - name: Greet user
        run: echo welcome to Actions
  
        
        
       
        
    
  
             
